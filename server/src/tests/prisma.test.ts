import { PrismaClient } from "@prisma/client";

const prisma = new PrismaClient();

const getCurr = async () => {
    
    try {
        
    } catch(error) {
        return error;
    }
}

export { getCurr };